A file in the environment (see [[Environment]]) that the harness (see [[Harness]]) loads into the context window (see [[Context window]]) at session (see [[Session]]) start — the project's standing brief to the agent (see [[Agent]]). Cross-harness convention.

*Avoid:* using AGENTS.md for content that should be **progressively disclosed** (see [[Progressive disclosure]]) — anything in it pays a token (see [[Token]]) cost every turn (see [[Turn]]).

*Usage:* "Why is every session starting with 4k tokens already burned?" "Check AGENTS.md — someone pasted the entire style guide in there instead of putting it behind a skill."
